{
inches = microsecondsToInches(duration);    //this is distance in inch

cm = microsecondsToCentimeters(duration); //distance in cm

Serial.print(inches);

Serial.print("in, ");

Serial.print(cm);

Serial.print("cm");

Serial.println();

delay(100);

}
